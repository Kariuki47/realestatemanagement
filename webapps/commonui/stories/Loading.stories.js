import { Loading } from '@realestatemanagement/commonui/components';

const page = {
  title: 'Loading',
  component: Loading,
};

export default page;

export const Default = () => {
  return <Loading />;
};
